---
galaxy_info:
  author: Alexandre Luedo
platforms:
  - name: Debian
    versions:
      - bullseye
      - bookworm
      - trixie
dependencies:
  - role: systemd
    vars:
      service_name: "{{ networkd }}"
...
