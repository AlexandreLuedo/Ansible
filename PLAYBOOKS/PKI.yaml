---
- name: Create a full PKI, root-ca, tls-ca, tls-certs
  hosts: serv3
  become: true
  roles:
    - New_PKI
  vars:
    # ROLES VARS => defaults/main.yaml
    organization_name: "My cloud"
    country_name: FR
...